'use strict';

// объявление с помощью const похоже на let,
// но значение является неизменяемым,
// поэтому при попытке присвоить другое значение, будет ошибка
const variable = 'initial value';
// variable = 'changed value'; // error